from setuptools import setup

setup(
    name='hyperloop'
    version='0.1',
    description='All the hype now',
    author='Chathura',
    author_email='cjgunase@mtu.edu',
    url='cjgunase.github.io',
    packages=['hyperloop'],
    install_requires=[
    'hypoloop',
    'geek'
    ]
)
